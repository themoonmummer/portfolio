The app is stuck on the pink page due to a Framer Motion context error ("Cannot read properties of null reading useContext"). 

Please perform the following fixes:
1. Fix the Framer Motion / React version mismatch or import issue so components mount without throwing runtime errors. Ensure hooks are imported cleanly from 'react'.
2. Update the Hero component animation flow:
   - Start with the pastel pink (#FFD1DC) background and typewriter "hello".
   - Once "hello" finishes typing, animate its scale up rapidly so "hello" grows super large and zooms past/out of the screen.
   - As it zooms out, smoothly fade out the pink background and zoom/fade IN the main Mount Fuji (`hero.jpeg`) background with the central glassmorphism card (containing "Riya Jha", "Vibe Coder & Full-Stack Developer" in Cormorant Garamond, and the vertical navbar).
3. Ensure all animation timers and state transitions complete properly so the layout never gets stuck.