Please refine the Hero component styling and transition animations as follows:

1. "hello" Text & Zoom Fixes:
   - Font & Style: Make "hello" bold and set its font-family to 'Times New Roman', Times, serif.
   - Initial Size: Increase the starting size of "hello" so it is distinctly visible and bold in the center of the screen during the typing animation.
   - Zoom Effect: When zooming in/out, scale "hello" up exponentially so it expands to consume the ENTIRE screen before fading out into the hero section.

2. Central Glassmorphism Card (Refining the 'frame' design):
   - Clear Glass Effect: The central card must look like ultra-clear, translucent frosted glass overlaying the Mount Fuji (hero.jpeg) background.
   - Styling Details:
     * Background: Semi-transparent white with low opacity (`rgba(255, 255, 255, 0.15)` to `0.25`).
     * Backdrop Filter: Apply `backdrop-filter: blur(16px) saturate(180%)` (and `-webkit-backdrop-filter`).
     * Border: A thin, crisp 1px light border (`rgba(255, 255, 255, 0.4)`) with smooth rounded corners (`border-radius: 20px` to `24px`).
     * Shadow: A soft, subtle elevation shadow (`box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.1)`).
   - Card Layout:
     * Keep the inside clean and minimal: Left side displays "Riya Jha" (Bold) and "Vibe Coder & Full-Stack Developer" in the elegant serif font.
     * Right side displays the vertical links: Home, Projects, About, Contact.
     * Ensure NO photo collage cluster or extra images are present inside or around the card.

3. Transitions:
   - Smoothly transition from the screen-filling "hello" zoom directly into revealing this polished glass card over the Fuji background.