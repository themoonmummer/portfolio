Please complete the following two tasks in the project:

TASK 1: Fix App.tsx Module Import Error
- Resolve the runtime error: "Uncaught SyntaxError: The requested module '/src/components/ProjectsSection.tsx' does not provide an export named 'ProjectsMetro'".
- Update `src/App.tsx` line 3 to use default import: `import ProjectsMetro from "./components/ProjectsSection";` (or ensure `ProjectsSection.tsx` correctly exports `ProjectsMetro` as a named export).
- Ensure explicit TypeScript typing for any callback parameters (such as `project: any` or `project: Project`) to eliminate TS7006 implicit 'any' warnings.

TASK 2: Implement Interactive Ferris Wheel About Section
Replace/update the About component to match the visual aesthetic and interaction pattern from `assets/inspo/From Klickpin.com- Fresh tiny apartment decor that combine popular trends with useful details you can actually use for your next inspiration board.gif`:

1. Visual Theme & Color Palette:
   - Palette: Soft pastel pink (#FFD1DC), pastel blue, pastel yellow, and soft lilac/purple with subtle Japanese aesthetic details (cherry blossom accents, minimal line art, vaporwave/lo-fi vibe).
   - Graphic Element: Build an interactive, spinning animated Ferris Wheel (replacing a bicycle wheel layout) in the center/side of the section using SVG/Framer Motion.

2. Ferris Wheel Interactive Nodes / Cabins:
   - Attach clickable interactive nodes/cabins on the Ferris wheel corresponding to three main categories:
     1. "About Me"
     2. "Education"
     3. "Experience"
   - Add continuous smooth rotation to the wheel. Hovering over a cabin pauses/highlights it, and clicking a cabin activates that category.

3. Content Panel Overlay / Drawer (Triggered on Click):
   - Clicking a node opens a clear pastel glass modal/card displaying the content:
     * About Me: Include the bio paragraph: "I'm a person with a lot of hobbies, and this is one of them where I want to create and earn through it so I can support my other hobbies and dreams too."
     * Education: Add structured placeholder items (e.g., Degree, Institution, Timeline) ready for editing.
     * Experience: Add structured placeholder items (e.g., Role, Projects Built, Key Skills applied) ready for editing.
   - Match the exact motion timing, smooth entry/exit transitions, and tiny decorative detail aesthetic seen in the reference GIF file.